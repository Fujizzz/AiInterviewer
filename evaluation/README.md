# Evaluation module placeholder

Reserved for the evaluation team's evidence extraction, behavior-anchored rubric scoring,
coverage/confidence updates and final competency aggregation.

The production module should implement the contract exposed by `agents/ports/evaluation.py`.
The provisional terminal-MVP adapter remains under `app/adapters/evaluation.py` until interface
integration is scheduled.
