# Frontend module placeholder

Reserved for the frontend team's code. No frontend implementation has been added yet.

Expected ownership:

- resume upload and interview views;
- real-time answer interaction;
- report presentation;
- client-side state and API client code;
- frontend tests and build configuration.

Keep `package.json`, lock files, source assets and frontend tests inside this directory. The
frontend should depend on the backend API contract, not import Python implementation modules.
