# Copy to backend/.env. Never put API keys in browser code or commit backend/.env.
# Existing process environment variables take precedence. Restart after editing.
LLM_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=
OPENAI_TEMPERATURE=0

# For Qwen, set LLM_PROVIDER=dashscope and fill both fields below.
DASHSCOPE_API_KEY=
DASHSCOPE_MODEL=qwen-plus
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# DJANGO_SECRET_KEY is separate from the model API key.
# Supply a randomly generated value here or through the process environment.
# DJANGO_SECRET_KEY=

# Optional resume vision Agent (independent of the interview model).
# Explicitly select provider/model; missing configuration is a visible error, no fallback.
# Reuses the selected provider's API_KEY and BASE_URL above.
RESUME_VISION_PROVIDER=
RESUME_VISION_MODEL=
RESUME_VISION_TIMEOUT_SECONDS=90
# DashScope only: set true/false explicitly if the chosen model requires it.
RESUME_VISION_ENABLE_THINKING=
