# 测试说明

## Agent 持久化与 Schema（2026-09-13）

本轮范围为后端数据库适配、请求生命周期、历史 API 和相关安全边界；Agent/App/共享契约及
评分、模型、时间预算和重试参数保持原样。数据库备份后应用 0004/0005，旧练习表逐行摘要相同，
SQLite foreign_key_check 无错误。新增请求索引补齐 ID 排序列；EXPLAIN QUERY PLAN 确认
请求历史使用复合索引且不再产生临时排序 B-tree；该检查不是吞吐或大数据规模性能基准。

新增 15 项持久化测试覆盖：新适配器读取已保存上下文、单轮末端约束失败全部回滚、
旧版本拒绝、跨连接 UUID 去重、同场单请求约束、未评分回答保留、正常中断与完成状态、
跨场问题/请求隔离、只读且禁止缓存的历史 API、存储失败不触发模型、响应保存早于网络交付。
既有 Agent 与终端 MVP 的结果一致性测试改用隔离测试库，不通过内存替身绕过持久化。
完整后端套件 69 项通过；面试/PDF 客户端 14 项及流式客户端 5 项通过；
根目录 MVP 为 79 项测试和 5 个子测试通过。

真实本机 HTTP/WebSocket 联调使用离线 FixtureLLM：完成两题并通过历史 API 读回回答及报告，
重连重发请求 UUID 被拒绝；预解析、进度和评分先行仍通过。WAV/echo 阶段仍验证数据库字节不变，
该断言现在准确限定在不应写库的诊断阶段，Agent 写库本身改为验证已保存数据。

原版注释检查器、Ruff、迁移一致性检查和根目录 MVP 回归通过；检查器未修改，
继续使用既有 Python 3.12.14 docs-env。人工核对事务边界、声明说明、目录、状态字段及页面存储提示。
本轮不调用付费模型，未实现/验证进程强制退出后的自动恢复、用户账户权限、PDF 硬隔离、
全局推理限额、PostgreSQL 部署或真实并发负载。尚未提交 Git，不声称提交原子性已经验证。

## 多页并发与结果展示验证（2026-09-13，最新流程）

每份 PDF 最多 3 页并发校对，完成后补充窗口；失败、取消或消费者关闭时，取消并等待
其余任务，再释放共享模型客户端。按原页序合并最终文本，进度按真实完成数量更新。
前端只显示最终文本和必要疑点，不显示传统提取中间结果、修改前后片段或依据；
HTTP 不再发送 `rules` 事件，也不再返回 `corrections`。内部校对补丁和校验规则保持不变。

- 54 项 Django/ASGI 测试通过，包含 17 项 PDF/视觉测试。新增事件闸门控制的并发测试，
  验证窗口上限、完成后补充、乱序完成、失败取消兄弟任务和生成器关闭清理，不靠睡眠估算。
- 8 项面试客户端及 6 项 PDF 客户端测试通过，覆盖最终文本、疑点、无修改明细、失败和取消。
- 生产 HTTP 配合真实 `qwen3-vl-flash` 完成三页简短合成 PDF；日志确认三页请求重叠执行，
  完成顺序为 3、2、1，最终 pages 和全文仍为 1、2、3。总耗时约 5.83 秒，返回无修改建议。
  此为一次小型样例观测，不代表真实简历吞吐、单页加速或多份上传的全局限流效果。
- Ruff、原版注释检查通过；覆盖 43 个 Python 模块/240 个声明、10 个 JavaScript 模块/
  175 个声明（46 个匿名回调）。沿用既有 Python 3.12.14 文档检查环境及原检查规则。
  人工核对调度生命周期、取消顺序、页面合并、注释目录和界面文案。

以上检查在提交前完成，代码、注释与文档随同一版本交付。下节为此前串行校对及修改建议展示的历史验证记录，当前流程以本节为准。

## 串联提取与视觉校对验证（2026-09-12，初版串行流程）

按需求纠正为一个入口、单次上传，必须先提取再视觉校对；已移除仅规则模式及其接口分支。
模型改为返回相对规则基线的 `before/after/reason` 补丁和疑点。无补丁时逐字保留基线，
有补丁时校验唯一来源与非重叠性后仅替换对应片段。失败和取消不能采用未完成校对的预览。

- 51 项 Django/ASGI 测试通过，包含 14 项 PDF/视觉测试：无修改保留空白、明确纠错、
  重复来源、错误来源、重叠补丁、空白理由、无效修改、扫描页补录及失败/取消。
- 8 项既有面试客户端和 6 项 PDF 客户端测试通过；验证单一上传字段、修改前后及依据展示、
  失败后禁止采用、无需调整状态、断流、取消及旧请求隔离。
- 真实模型：生产 HTTP 完成合成单行 PDF 的提取与校对，无需调整时输出与规则基线逐字一致；
  同页图像配合人为制造的 `Pyth0n` 提取错误，经真实 Agent/模型校对只改为 `Python`，其余内容不变。
  本轮没有重新评测复杂扫描件准确率，也没有使用用户真实简历作为验证数据。
- Ruff 通过；原版注释检查覆盖 43 个 Python 模块/233 个声明和 10 个 JavaScript 模块/
  175 个声明（46 个匿名回调），问题数 0。延用既有 Python 3.12.14 检查环境。
  人工核对新增 Agent 补丁契约、注释、目录和页面流程；代码尚未提交 Git。

下节保留初版双入口实现的历史记录，其模式选择和转写行为已被本节流程取代。

## PDF 规则提取与视觉整理验证（2026-09-12）

新增独立的 `agents/resume_cleanup.py` 与后端提取、渲染、模型适配、HTTP 阶段流及前端人工采用入口。
没有修改既有面试策略、评分公式或预算；视觉模型使用独立配置，本机设为 DashScope
`qwen3-vl-flash`、非思考模式，复用现有凭据。

- 50 项 Django/ASGI 测试通过，包含新增 13 项 PDF、视觉端口、HTTP 契约及取消测试。
- 13 项面试/PDF 客户端测试及 5 项既有流式客户端测试通过；PDF 测试使用真实脚本、
  分片 UTF-8 流及 DOM 替身，覆盖人工采用、断流、失败、取消后迟到响应和面试锁定。
- 根目录 MVP：79 项测试、5 个子测试通过。
- 真实 HTTP/WebSocket、原有两题面试、预解析复用、先行评分、WAV 和双客户端回传通过，
  Agent/传输阶段前后 SQLite 字节一致。测试清理曾发生 WinError 32；
  修复为 taskkill 前捕获后代进程句柄、之后等待全部退出，复跑退出码 0，原业务断言不变。
- 真正 DashScope 视觉请求经生产 HTTP 上传入口完成两份虚构简历：文本型英文双栏单页
  约 5.07 秒，中文双栏扫描单页约 3.66 秒。扫描件规则文本为空，视觉结果保留中文、
  Python/SQL、2024.06—2025.03 和 30%，人工对照渲染图核对一致。
  这些为单次、小型合成样例的观测值，不是复杂简历准确率或性能承诺。
- 已检查浏览器新增导入区初态排版、实际 PDF 渲染图及逐页转写。未通过原生文件选择器
  完成浏览器点击上传全链路；上传接口与客户端状态机分别通过实际 HTTP 和脚本测试验证。
- Ruff、迁移一致性、git diff --check 通过。原版注释检查器覆盖 43 个 Python 模块/232 个声明，
  10 个 JavaScript 模块/175 个声明（45 个匿名回调），问题数 0。
  延用下节所述 Python 3.12.14 文档检查环境及固定依赖，未修改检查器或标准。
  人工核对新增 Agent 文件、HTML/CSS 及所有本轮改动的功能说明、状态和目录；
  后端检查器不扫描根目录 Agent 文件，未声称它属于自动检查覆盖范围。

完整入口与限制见 [PDF 简历解析](resume-pdf.md)。未评测低清照片、复杂表格、
长文档并发、恶意 PDF 的硬资源隔离、其他视觉模型或账号权限。代码尚未提交 Git。

## 等待优化验证（2026-09-12）

本轮仅修改 backend 的适配、协议、界面和相关测试/文档；Agent、App、共享契约与实验参数未改。
复测结果：37 项 Django/ASGI、8 项面试客户端状态测试、5 项既有流式客户端测试通过。
真实 ASGI/WebSocket 联调覆盖原有两题流程、新版 prepare→start→answer→assessment→finished、
HTTP、WAV 和双客户端回传；业务数据库在 Agent/流式阶段前后逐字节一致。
人工核对 prepare 缓存失效、started/阶段事件顺序、评分公式复用、取消清理与报告回退标记。
这不是实际模型速度基准，本轮未调用付费模型。

后端 Ruff、git diff --check 通过。注释检查覆盖 39 个 Python 模块/201 个声明，
8 个 JavaScript 模块/138 个声明，问题数 0；检查器既有 22 项回归也通过。
生产和业务测试继续使用现有 Python 3.12.7。该解释器运行完整 Tree-sitter 检查时发生原生
访问冲突（faulthandler 定位于 GC/语法树遍历）；简单解析与部分检查器用例正常。
使用既有文档记录的 Python 3.12.14，在 `test-results/docs-env/` 安装相同固定
`requirements-docs.txt` 后执行原版 `python tools/check_docs.py` 和检查器回归通过。
没有修改检查器、依赖版本或检查规则；不据此声称生产环境已修复原生解析问题。

Windows 联调清理曾因虚拟环境 python 启动器退出、实际服务子进程仍持有日志而失败。
`run_e2e.py` 现明确结束本次启动的进程树，再清理临时目录；原业务断言保持不变，复跑退出码 0。
代码、注释与目录已同步核对，尚未提交 Git，不声称已验证提交原子性。

新增客户端检查命令：`node --test tests/agent-client.test.mjs`。
下文保留 2026-09-11 的历史验证范围和观测值。

验证日期：2026-09-11。Python 3.12.14；Node.js 24.19.0；Codex 内置 Chromium 浏览器。这些版本记录用于说明已有测试环境，不要求使用特定环境管理工具。

## 可复现命令

在 backend 目录，使用已安装本目录 `requirements.txt` 依赖的 Python 环境执行：

```powershell
python -m pip install -r requirements-docs.txt
$env:DJANGO_SECRET_KEY = python -c "import secrets; print(secrets.token_urlsafe(48))"
python manage.py test interviews
python manage.py makemigrations --check --dry-run
python tools/check_docs.py
python -m unittest discover -s tools -p "test_*.py"
python tests/run_e2e.py
python tests/run_agent_e2e.py
```

Django 测试使用隔离数据库；`run_e2e.py` 创建临时 SQLite 文件和临时本机端口，迁移后启动实际 Uvicorn 服务，退出时停止该子进程并清理临时文件。
不会改写开发数据库，不依赖真实摄像头、麦克风或云端服务。Node.js 22+ 必须可通过 PATH 找到。

## 语法树注释检查强化验证（2026-09-11）

本次在 backend 范围内以 Tree-sitter 替换 JavaScript 正则识别，并将匿名回调纳入双位置目录检查。前端源码只调整注释，生产参数和失败语义保持不变。

| 层次 | 结果与范围 |
| --- | --- |
| 注释检查 | 38 个 Python 模块、176 个类/函数定义；7 个 JavaScript 模块、101 个函数/类声明（含 39 个匿名函数），问题数 0 |
| 检查器回归 | 22 项通过；覆盖两处注释四种组合、限定名、匿名函数、错误目录、模板插值、多行与对象方法、空 JSDoc、缺失解析器和进程退出码 |
| 后端与前端回归 | 30 项 Django / ASGI 测试、5 项 JavaScript 客户端测试通过；实际 HTTP、离线 Agent WebSocket、WAV 及双客户端联调通过，SQLite 字节不变 |
| 静态与迁移 | backend Ruff 检查通过；无模型迁移变化 |

匿名编号依赖同层函数的源码顺序，调整顺序后仍须人工确认条目语义；检查器不能证明代码和注释在 Git 提交中的原子性。开发依赖固定在 `requirements-docs.txt`，缺失时明确失败；没有添加正则回退或修改仓库共享 CI。下列章节保留前期实现的历史验证记录，旧计数与旧检查边界不代表当前实现。

## 注释规范完善验证（2026-09-11，语法树强化前）

本次修改范围仅为 `backend/`。以下是本次复验结果；后续章节保留各阶段的历史记录，其数量和环境不代表本次重新验证的范围。

| 层次 | 结果与范围 |
| --- | --- |
| 注释结构检查 | 36 个 Python 模块、152 个类/函数定义；7 个 JavaScript 模块、50 个可识别具名声明，缺失 0。核对文件目录、模块变量索引与实际声明，并检查函数文档 |
| 检查器回归 | 8 项通过，覆盖嵌套定义、变量作用域、失效目录、重复/空说明、JavaScript 声明识别、依赖目录排除及语法错误 |
| Django / ASGI | 30 项测试通过；无模型迁移变化 |
| 客户端与端到端 | 5 项 JavaScript 测试通过；真实 HTTP、离线 Agent WebSocket、WAV 回传及两个并发 StreamClient 连接通过；SQLite 文件逐字节不变 |
| 静态检查 | backend 范围 Ruff 通过；5 个前端 JavaScript 文件语法检查通过 |
| 行为保持核对 | 去除文档字符串后的 Python AST，以及去除注释后的 JavaScript、HTML 和 CSS 对比显示，45 个既有实现文件未发生业务代码变化；实现变更仅涉及 `tools/check_docs.py`，另新增 `tools/test_check_docs.py` |

检查器无需导入业务模块或读取密钥。JavaScript 检查是有限的静态识别，匿名回调、模板插值内定义、行内对象方法和多行方法签名仍需人工核对；注释语义及代码与注释的原子更新也需在评审中确认，详见 `code-guide.md`。本次未调用真实付费模型，未重新执行 backend 以外模块的独立测试。

## 原练习与流式诊断验证记录

| 层次 | 结果与范围 |
| --- | --- |
| Django / ASGI 自动化 | 20 项通过：业务接口与协议边界，新增 100/101 题边界、显式子集和拆分资源检查；流式测试使用禁止数据库访问的 SimpleTestCase，并在禁止文件打开和 SQLite 连接时完成回传 |
| 前端客户端自动化 | 5 项通过：损坏回传、错误 ACK、最终计数不匹配、超时和主动取消 |
| 真实 HTTP | 场次创建→开始→提交答案→结束→查询成功；并发相同版本的两次更新分别返回 200、409 |
| WAV 音频 | 8 个分片 / 32,044 bytes，内存中原样回传，重新解码为 16,000 音频帧 |
| 实际前端 StreamClient | 两个并发连接，各 6 个不同尺寸分片 / 1,131,008 bytes，逐字节一致；进度在关闭前到达 |
| 不落库验证 | 实际 WAV 和并发前端流式测试前后，SQLite 文件逐字节完全相同 |
| 浏览器 ping/pong | 收到匹配响应，单次实测 RTT 5.7 ms |
| 浏览器二进制 | 12 个分片 / 393,216 bytes，全部通过 SHA-256 |
| 浏览器合成音视频 | 约 3 秒，11 个分片 / 96,893 bytes，通过 SHA-256；回传 WebM 解码为 640×360，readyState=4 |
| 浏览器清空结果 | 回放 src 为空、readyState=0、字节和分片计数归零、页面日志为空 |
| 注释覆盖 | 28 个 Python 模块、95 个类/函数、6 个 JavaScript 模块，缺失 0 |
| 迁移与静态检查 | 无模型迁移变化；整个仓库 Ruff 通过；重构入口 JavaScript 语法检查通过 |
| 既有 Agent MVP | 根目录独立环境：79 项测试、5 个子测试通过；未修改 Agent 实现、策略和依赖 |

根目录检查使用 `uv sync --extra dev --locked` 后的环境，运行 `uv run pytest -q` 与 `uv run ruff check .`。

## MVP Agent 接入验证（2026-09-11）

- Django / ASGI 共 30 项测试通过，包括新增 10 项 Agent/供应商测试。
- 离线模型替身配合真实 Agent 核心，验证网络与终端 MVP 的问题文本、能力状态、预算和最终报告一致。
- 验证输入校验、重复请求、旧问题、忙碌状态、会话隔离、取消、断线、脱敏错误、缺失模型配置及在途客户端释放。
- `run_agent_e2e.py` 启动显式离线测试入口，在真实 WebSocket 上完成两题面试，并回归 HTTP、WAV 和前端客户端；Agent 与流式测试前后 SQLite 字节一致。
- 浏览器离线测试完成简历提交、两题作答和 3.00/5 报告展示；清空操作移除简历、答案及报告。模型输出明确标识为离线测试数据。
- 根目录 MVP 回归：79 项测试、5 个子测试通过；后端依赖检查、迁移一致性和文档覆盖检查通过。
- 真实千问调用已于 2026-09-11 验证：使用 `backend/.env` 与生产 ASGI 入口，经实际 WebSocket 提交虚构简历并完成一题面试，全程约 20.1 秒。日志确认简历解析、问题生成、回答评价及报告文字四个阶段均成功；预算仍按 MVP 扣除 120 秒。此结果仅覆盖本次配置与单题流程，不代表多轮、负载或所有模型均已验证。

测试专用 `interviews.tests.agent_fixture_app` 只在显式指定时加载，生产入口没有模拟模型开关或隐式回退。

以上时延和分片数是一次本机验证的观测值，不是性能保证。MediaRecorder 调度不精确，不使用分片数量推算录制时长。

## 浏览器复验

1. 按 README 启动服务，从 http://127.0.0.1:8765/ 主页进入传输诊断，或直接打开 http://127.0.0.1:8765/stream-demo/。
2. 点击 Ping / Pong，检查匹配响应及 PASS。
3. 点击二进制分片测试，确认结束前计数增长，最终 12 个分片、393216 bytes。
4. 点击合成音视频测试，等待录制结束，确认 PASS；点击回传播放器验证媒体播放。
5. 点击“清空结果与媒体缓存”，确认回放源、统计与页面日志清空；刷新后没有历史结果。

真实麦克风/摄像头入口已实现，但本次没有请求真实设备权限；硬件质量、权限拒绝界面和跨浏览器编码兼容性需要在目标设备进一步验证。
浏览器测试证明本地音视频分片可双向回传并重组；不代表 WebRTC、低延迟远端播放、生产并发负载或公网网络已经验证。
