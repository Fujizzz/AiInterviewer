# RAG module placeholder

Reserved for resume/job/technical knowledge ingestion, retrieval, reranking and index management.

The production module should implement the contract exposed by `agents/ports/rag.py`. It should
not make interview policy decisions; source selection and retrieval timing remain Agent concerns.
